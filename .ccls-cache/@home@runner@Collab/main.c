#include <stdio.h>

void whatILearned() {   

  int UIN= 675221328; 
  char netID[] = "mcirin3";

  printf("Two things that I learned with using and git and github was that git is for local \n"); 
  printf("version control and github is for cloud collaborative version control. This means that the \n"); 
  printf("multiple people can be working on the same document at the same time! \n");  
  printf("UIN: %d", UIN); 
  printf("\n");
  printf("netID: %s", netID);
  
  }
int main(void) {
  whatILearned();
  return 0;
}